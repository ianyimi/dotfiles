# Harness Setup Report — dotfiles
> Written by `harness init finish` at commit 58a2408. Review it, then ask the
> agent questions, correct errors, or request changes — everything here is adjustable.

## What was configured

- **Platforms**: omp, claude — bridges generated by `harness sync`
- **Workflow**: low-care, importance medium, commit mode `message-only`
- **Standards domains**: shell, infrastructure, tooling
- **Modules on**: specs, tasks, session_log, roadmap, decisions, research, env_manifest
- **Modules off**: design, naming_conventions
- **Pinned dependencies**: chezmoi, pure-bash-bible, google-shell-styleguide, zsh, ansible, bitwarden-clients, cachyos-settings, pacman

## How agents will use this harness

- Every session starts with `harness doctor` (auto via the platform hooks) and `harness state`.
- Workflows are skills in `.agent/skills/` — invocable as slash commands (`/harness-init`,
  `/dev-spec`, `/sync-spec`, `/commit`, `/summary-prompt`, …) or by natural language.
- The harness-keeper advisor is ON (OMP): a cheap watcher that turns your feedback
  into harness updates, logged in `docs/harness-changelog.md` and relayed back to you.
- Before touching code, agents query the code map (`harness context --for "<task>"`) to load
  exactly the standards that govern the files they will change.
- Learnings flow back into the harness (see `docs/harness-guide.md`, "Where knowledge goes"),
  and `harness sync` recompiles the map after standards change.
- `/commit` runs the project's commit checklist
  (`.agent/skills/commit/references/commit-checklist.md`) before anything ships upstream.

## What to review now

- `docs/harness-guide.md` — how this harness is organized
- `docs/standards/` — the practices recorded for this project (discovery findings below)
- `docs/standards/naming-conventions.md` — the naming rules agents will enforce
- `.agent/skills/commit/references/commit-checklist.md` — your pre-push gate

## What discovery found

Three parallel domain sweeps (shell / infrastructure / tooling), all claims verified against
code with file:line evidence. 10 standards files written:

- **shell/**: `aliases.md` (dot_zshrc.tmpl is the alias source of truth; cd/nv/lg verb-prefix
  triples; `cma` is the canonical deploy — never bare `chezmoi apply`), `scripts.md` (bash 3.2
  compat, `set -e` only, idempotency guards, ✓/→/⚠/✗ output glyphs, ~/.bw-session plumbing),
  `chezmoi-templating.md` (golden rule: edit only in source repo; OS conditionals; Bitwarden
  lookups; `.chezmoiignore` per-OS gating; opaque-dir sync pattern), `debugging.md` (migrated
  from `.pi/agent-docs/standards/debug-hierarchy.md`, corrected).
- **infrastructure/**: `provisioning-playbooks.md` (flat plays, `Check Install -` naming, four
  idempotency patterns, become-per-play rules, `# VERIFY` marker convention),
  `adding-packages.md` (both-playbooks rule + placement decision table, `-bin` AUR preference),
  `bootstrap-flow.md` (prereqs → chezmoi → Tailscale → Bitwarden → init → apConfig ordering
  contract; run_once scripts are safety nets, not owners).
- **tooling/**: `harness-typescript.md` (Bun-from-source, `.ts` import extensions, props-object
  style, HarnessError/EXIT contract, stdout/stderr sink params), `harness-testing.md` (hermetic
  bun:test, in-process runCli, fixture dirs, golden assertions), `pi-agent-sync.md`
  (pi-agent-base → ~/.pi/agent rsync contract, preserved runtime state, generated .omp/.claude
  bridges are never hand-edited).

### Old-harness content migrated (verify-then-adopt)

| Old file | Verdict | Landed in |
|---|---|---|
| `.pi/agent-docs/product/tech-stack.md` | corrected (ansible paths, CachyOS first-class, agent-os dropped, harness/ added) | `docs/product/tech-stack.md` |
| `.pi/agent-docs/product/dev-processes.md` | verified + extended (harness dev commands) | `docs/product/dev-processes.md` |
| `.pi/agent-docs/product/mission.md`, `roadmap.md` | corrected (CachyOS, harness rollout) | `docs/product/mission.md`, `roadmap.md` |
| `.pi/agent-docs/standards/debug-hierarchy.md` | corrected (dropped nonexistent `darwin//linux/` dirs, dead ideaLog path → session log) | `docs/standards/shell/debugging.md` |
| `.pi/agent-docs/standards/developer-preferences.md` | dropped — empty scaffold | — |
| `.pi/AGENTS.md` | verified (aliases re-checked against dot_zshrc.tmpl) | `shell/aliases.md`, `shell/chezmoi-templating.md` |
| `.claude/commands/` (commit, debug, dev-spec, document, learn, research, review, sync-spec, harness-init) | superseded by harness skills | — |
| `.claude/commands/` (add-vexfield, feature-checklist, guide, copy-commit*) | dropped — foreign to this repo / dead `agent-os/` paths | — |
| `agent-os/` (product + standards) | dropped — stale generic boilerplate, 6mo old | — |

### Coverage gaps deliberately left (inbox follow-ups filed)

- `dot_config/nvim`, `wezterm`, `sketchybar`, `noctalia` config internals — app-config bodies,
  low change-frequency; covered generically by the chezmoi-templating golden rule.
- Hyprland `config/*.lua` bodies — structure rule recorded; per-topic conventions unexplored.
- `reset.sh` tail + remaining `dot_local/bin` helpers — same shell conventions, spot-verified.

### Notes

- Dependency clones: chezmoi/zsh/ansible/bitwarden-clients pinned to exact tags;
  pure-bash-bible, google-shell-styleguide, cachyos-settings, pacman cloned at default branch
  (no matching tags — expected for untagged repos).
- Context map spot-checked: `dot_zshrc.tmpl` → shell standards, `dot_bootstrap/cachyos.yml` →
  infrastructure standards, `harness/src/**` → tooling standards.
- `docs/standards/naming-conventions.md` was not generated (module off, low-care skip).
- Cleanup (developer-approved): deleted 14 old `.claude/commands/*.md`, the unmanaged
  `.claude/skills` dir, `agent-os/`, `.pi/agent-docs/product/`, `.pi/agent-docs/standards/`,
  and `.pi/AGENTS.md` after migration. `harness sync` regenerated managed replacements;
  `harness doctor` green with 0 conflicts. Kept: `.pi/prompts/`, `.pi/agent-docs/specs/`,
  `.pi/agent-docs/implementation-log/` (history, copied into `docs/session-log/`),
  `pi-agent-base/` (deployed payload).
