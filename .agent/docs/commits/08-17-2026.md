# Commits — 2026-08-17

## 06:45

```
feat: nvim redraw perf, harness fetch/critique, nvim subproject

**Nvim editor performance.** Any full-screen redraw with a large markdown spec visible cost ~1.1s:
Nvim's highlighter re-resolves injected regions roughly once per visible line, each pass walking
every child tree — 461 regions in a 3761-line spec. Markdown/mdx buffers above
`vim.g.markdown_ts_highlight_max_lines` (1500) now skip the treesitter *highlighter* while keeping
the parser reachable, so render-markdown still renders and regex syntax keeps headings and fenced
blocks coloured. The gate lives in both `after/ftplugin/markdown.lua` and
`lua/plugins/editor/treesitter.lua` (`HEAVY_INJECTION_FT` / `skip_highlighter()`) because Nvim
0.12's bundled `ftplugin/markdown.lua` calls `vim.treesitter.start()` unconditionally, before any
FileType autocmd runs — `after/ftplugin/` is the only override point that runs late enough.

Alongside it, four more main-loop costs: satellite's `handlers.render` spawns an async coroutine per
handler per window and every step costs a `vim.schedule`, so `current_only = false` produced 3455
callbacks totalling 29s in a 62s session — draw only the focused window and drop the gitsigns
handler, whose information the gutter signs already carry. indent-blankline caches one uv timer per
buffer number with no BufDelete handler at all, leaking one permanently per Telescope preview
buffer; release it on BufDelete/BufWipeout. lazy.nvim's update checker leaked a timeout timer per
spawned `git fetch` across ~67 plugins and accounted for 173 of 192 open handles — disabled outright
rather than quieted, since `notify = false` would not stop the handle growth. chezmoi.nvim's
edit-watch ran `chezmoi status` through a blocking `plenary Job:sync` on every buffer read under the
source directory and was registered twice, so opening anything in this repo paid ~117ms twice before
the buffer appeared; de-duplicated and deny-listed. auto-dark-mode's poll went 1s → 60s after
spawning `defaults read` 191 times for 2483ms in a 192s session, to detect a change that happens
twice a day.

Measured across comparable sessions: worst stall 1129ms → 107ms, scheduled callback time 33.2s →
4.0s, Oil float 5s → 18ms, RSS 437MB → 139MB, idle libuv timers 300 → 53. Headless profiling could
not find any of this — redraw cost is invisible to it and redraw was the entire cause — so
`lua/util/perf-probe.lua` detects stalls via timer lateness inside a live session and attributes
libuv handles, `vim.schedule`, `vim.defer_fn`, `LanguageTree:parse` and decoration providers to
their creation sites. `plugin/perf-probe.lua` registers the `:Perf*` commands eagerly so the 34KB
module stays unloaded until first use.

**Markdown injections and plugins.** Fenced json/yaml/bash/shell blocks now inject; the custom
`datacorejsx` matcher is gone and redundant `priority 90` overrides dropped, leaving only dataviewjs
(110) and datacoretsx (120). mdx TypeScript now also matches `tsx`/`typescript.jsx`. The added cost
(+32 regions, ~+28% re-resolution) is documented inline, since injection count is exactly what the
highlighter gate bounds. `nvim-treesitter` moves `master` → `main` (master archived 2026-04-03),
`diegoulloao/nvim-file-location` adds `<leader>fy` for the workdir-relative path, and 30 other
plugins are bumped.

**Nvim becomes its own harnessed subproject.** `dot_config/nvim/.agent/` — manifest, AGENTS.md, 13
skills, standards for plugins/lua-style/keymaps, product docs — replaces the Claude-specific
`.claude/agents/agent-os/*`, `.claude/commands/agent-os/*`, 16 `.claude/skills/*`, `AGENTS.md` and
`claude.md`. The harness is deliberately unreachable by chezmoi: source entries beginning with `.`
are not managed, so `~/.config/nvim` never receives it and agents edit in this tree and deploy
selectively. A harness-managed block in `dot_config/nvim/.gitignore` drops `.claude/` and `.omp/`
from git.

**`harness fetch sync` — previewing bridge regeneration.** `harness sync` could only be run, not
inspected, so `harness fetch` (added previously) could report incoming *skill* changes but was blind
to the bridge files sync rewrites. `sync.ts` now splits: `planSync()` computes every filesystem
operation as a `SyncPlan` of typed `SyncChange`s — create/update/merge/symlink/delete/gitignore/
conflict/unchanged — without touching disk, and `applySync()` executes one; `runSync()` is the
composition of the two. `fetch sync` renders that plan, the bare `fetch` summary now also counts
actionable bridge changes, and `fsx.checkSymlink()` supplies the read-only counterpart to
`ensureSymlink()` the planner needs. CONFLICT entries name developer-modified managed files sync
refuses to touch, with the resolution (move the edit into `.agent/`) printed alongside. The
`harness-pull` skill is rewritten around fetch → merge-by-hand → sync instead of blind refresh.

**`harness install` anchors to the invocation directory.** It previously called
`resolveProjectRoot()` and walked up, so running it inside `dot_config/nvim/` adopted this repo's
outer `.agent/` and no-opped — a nested project could never bootstrap its own harness. Since
`install` is what *creates* the `.agent` marker that defines a root, walking up is always wrong;
it now uses `cwd` unconditionally. This is the change that made the nvim subproject above possible.

**New `critique` skill.** Ground-then-judge code review: read full files and LSP references before
forming opinions, load the project contract (anti-patterns, preferences, standards), run the
targeted verify command and quote its output, walk a silly-mistake catalog (async handlers missing
`return`, inverted guards, early loop exits, casts papering over contract changes, doc drift), then
report severity-ordered.

Why:
- Wall clock, not HEAD commit time, is now the source of truth for "now" in `log.ts`
  (`resolveLogDate`) and `spec.ts` (`specNew`), superseding D02-2. The commit-date rule stranded
  every session on a stale date until the day's first commit, and `spec new` could hard-fail
  `no-date` in a repo with no commits.
- Task-group ids become `Step <n>`; `GROUP_HEADING_RE` still accepts legacy `T<n>` so existing
  spec-tasks files keep parsing.
- The markdown gate keeps the parser alive rather than disabling treesitter, because render-markdown
  and treesitter-context read the tree even when nothing is highlighted.
- `dot_zshrc.tmpl` had the bun installer block appended twice with a hardcoded `/Users/zaye`
  completions path; collapsed to one `$BUN_INSTALL`-relative block, which also fixes it on CachyOS.

Spec: none
Log: .agent/docs/session-log/2026/08/2026-08-17.log.md
```
