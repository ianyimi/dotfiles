# GENERATED by harness index rebuild
root:
  directory-structure: { description: "GENERATED by `harness struct`. Do not edit manually.", lines: 1840 }
infrastructure:
  README: { description: "(first paragraph missing — add one)", lines: 1 }
  adding-packages: { description: "A package MUST be added to **both** playbooks, or the omission commented (nodejs comment,", lines: 21 }
  bootstrap-flow: { description: "`bootstrap.sh` is the curl-able entry point (README.md:9-10), fetched raw from GitHub —", lines: 29 }
  provisioning-playbooks: { description: "`hosts: localhost`, `connection: local`, explicit `become:` per play. Plays grouped by", lines: 29 }
shell:
  README: { description: "(first paragraph missing — add one)", lines: 1 }
  aliases: { description: "snapshot (Windows-era paths, dot_bashrc:37) — never add or port aliases there.", lines: 23 }
  chezmoi-templating: { description: "`~/.config/**`, `~/.bootstrap/**`, `~/.pi/agent/**`). Deploy with `cma`.", lines: 30 }
  debugging: { description: "1. **Terminal output** — chezmoi template errors (syntax, undefined vars, Bitwarden lookups),", lines: 28 }
  scripts: { description: "No `set -u`/`pipefail` — scripts rely on unset-var checks (`[ -n \"$BW_SESSION\" ]`), and", lines: 30 }
tooling:
  README: { description: "(first paragraph missing — add one)", lines: 1 }
  harness-testing: { description: "beside the source (src/lib/fsx.test.ts); cross-command flows in `test/e2e.test.ts` +", lines: 23 }
  harness-typescript: { description: "(package.json:6, cli.ts:1). No build step; `noEmit` + `allowImportingTsExtensions`.", lines: 27 }
  pi-agent-sync: { description: "`run_after_sync-pi-agent-base.sh.tmpl` deploys it to `~/.pi/agent` after every apply in two", lines: 26 }
