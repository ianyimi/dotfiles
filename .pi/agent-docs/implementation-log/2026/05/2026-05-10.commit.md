## Commit — 2026-05-10 23:29

chore: add keyboard constraints doc, oMLX bootstrap, code-signing, aliases

Added comprehensive ZSA Voyager keyboard layout and modifier key documentation to
AGENTS.md so future keybinding decisions reason against physical constraints (hold-tap
modifiers, AeroSpace Alt saturation, exhausted chord space, recommended priorities).

macOS bootstrap gains an oMLX LLM server installer from GitHub releases (DMG, not
brew formula — avoids Python 3.11/expat toolchain breakage on macOS 26), an Xcode
Command Line Tools pre-flight check that fails early with clear remediation, and a
code-signing stabilization stage that detects self-signed "zaye-codesign" identity,
bootstraps a resign LaunchAgent, and re-signs sketchybar/aerospace binaries to keep
accessibility permissions across `brew upgrade`.

Shell and TUI: added zsh aliases for tailscale reinstall and CLT upgrade, keybind
cleanup in tmux (copy-mode binding, removed stale version guards), and expanded
sketchybar app icon map (new apps, German translations, category fixes).

Refactored Tailscale install bootstrap to delegate to shared
`reinstall-tailscale` executable script.

