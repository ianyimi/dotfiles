## Commit — 2026-04-21 22:54

feat(pi): add pi agent to ansible playbook with custom extensions

Added Pi coding agent installation to Ansible provisioning with custom
vim-powerline combined extension and forked pi-image-preview that supports
spaces in filenames. Removed conflicting packages (pi-vim, pi-powerline-footer)
that are replaced by the local combined extension in pi-agent-base/.

Also added pi-agent-base/ and .pi/ to chezmoiignore to prevent chezmoi from
deploying Pi's runtime files (auth.json, sessions/, git packages).

Split claude CLI alias into two separate profile commands (claude-maprios,
claude-zaye) with distinct config directories for better API key isolation.
