## Commit — 2026-05-10 22:44

docs(keyboard): document ZSA Voyager hardware constraints and keybind rationale

Added comprehensive keyboard hardware documentation to AGENTS.md covering:
- Physical layer layout, modifier keys, and hold-tap semantics
- Alt+Z dedicated prefix key and its usage for tmux/vim navigation
- Modifier saturation in Aerospace and why Alt+key combos are unavailable
- Key notation cross-reference (alt/opt/M- and variant spellings)
- Layer 2 arrow cluster (hold Enter + IJKL)
- Exhaustive rationale for why keyboard shortcuts are saturated

This serves as the ground truth reference for all keybinding decisions across tmux, Neovim, Aerospace, Pi, and shell.

---

refactor(bootstrap): consolidate CLT checks, add oMLX, stabilize accessibility code-signing

dot_bootstrap/macos.yml:

Added Xcode Command Line Tools outdated detection with clear remediation:
- Pre-checks brew doctor for outdated CLT before source-built formulas (rust, llvm, python@3.14, omlx, etc.)
- Fails early if CLT is too old with explicit instructions and verification command

Added oMLX installation from GitHub releases (DMG, not brew formula):
- Installs via get_url → hdiutil mount → cp to /Applications
- Queries GitHub releases API for latest version
- Auto-selects DMG matching current macOS major version (macos26-*.dmg)
- Removes downloaded DMG after install
- Reports installed version after successful install
- Uses standalone app to avoid python@3.11 bottle issues on macOS 26

Added Accessibility code-signing stabilization playbook:
- Re-signs sketchybar and aerospace with stable self-signed identity (zaye-codesign)
- Ensures LaunchAgent is bootstrapped into GUI session if missing
- Warns if zaye-codesign identity not found in login keychain with one-time setup instructions
- Verifies signature state after resign

dot_zshrc.tmpl:
- Added tsreinstall alias to re-run standalone Tailscale install/migration (idempotent)
- Added clt-upgrade alias to wipe and reinstall Xcode Command Line Tools after macOS bumps

run_once_before_015_install_tailscale.sh.tmpl:
- Simplified from 49-line inline install to single include of dot_local/bin/executable_reinstall-tailscale

---

feat(localization): add German and multilingual app icon mappings

dot_config/sketchybar/helpers/app_icons.lua:
- Added 60+ new German translations (Aktivitätsanzeige, Bücher, Rechner, Kalender, etc.)
- Added multilingual support for additional languages (Japanese: マップ, Chinese: 日历/微信, Spanish: Calendrier)
- Merged duplicate mappings and standardized naming conventions

This improves icon recognition for German users and broader internationalization.

---

chore(tmux): simplify copy mode binding without version checks

dot_tmux.conf:
- Removed old conditional bind-key for Tcopy-mode-vi (2.4+) with version checks
- Added simple bind c copy-mode
- Modern tmux natively supports copy-mode binding; version checks no longer needed