# Mission

A fast, stable daily-driver Neovim setup, evolved incrementally from a stripped-down LazyVim
starter. Every plugin earns its place; migrations replace rather than accumulate (old plugins
are disabled, then removed). Custom cross-plugin glue — especially the harpoon–barbar
buffer-order link — and startup performance are the config's crown jewels; changes must never
silently drop existing functionality.
