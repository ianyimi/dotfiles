# Keymaps standards

Where keybindings live (eager config/keymaps.lua vs lazy spec `keys` tables vs LSP on_attach), the `[X]`-bracketed desc convention, the full `<leader>` namespace ownership map, reserved navigation planes (S-h/l, C-h/l, a-z prefix), and known collision tradeoffs.
