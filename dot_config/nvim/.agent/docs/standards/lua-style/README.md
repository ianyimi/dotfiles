# Lua style standards

Lua coding style for the whole config: mixed-indentation reality (match the file; new files 2-space stylua defaults), kebab-case file naming, `local M = {}` modules, snake_case symbols, and the `_G.LazyVim` util-library conventions (lazy-require, pcall + LazyVim.notify error shape).
