# GENERATED by harness index rebuild
root:
  directory-structure: { description: "GENERATED by `harness struct`. Do not edit manually.", lines: 315 }
  preferences: { description: "Learned working preferences. Format: `- P-NNN (date) rule`.", lines: 8 }
keymaps:
  README: { description: "Where keybindings live (eager config/keymaps.lua vs lazy spec `keys` tables vs LSP on_attach), the `[X]`-bracketed desc convention, the full `<leader>` namespace ownership map, reserved navigation planes (S-h/l, C-h/l, a-z prefix), and known collision tradeoffs.", lines: 3 }
  leader-namespaces-and-collisions: { description: "Leader is `<space>`. Before adding a `<leader>X...` map, place it in the right namespace.", lines: 44 }
  placement-and-structure: { description: "void-register cuts) → `lua/config/keymaps.lua`. It is required eagerly at", lines: 36 }
lua-style:
  README: { description: "Lua coding style for the whole config: mixed-indentation reality (match the file; new files 2-space stylua defaults), kebab-case file naming, `local M = {}` modules, snake_case symbols, and the `_G.LazyVim` util-library conventions (lazy-require, pcall + LazyVim.notify error shape).", lines: 3 }
  autocmds: { description: "`augroup()` (config/autocmds.lua:3) wraps `nvim_create_augroup(name, { clear = true })`, so", lines: 130 }
  indentation-and-formatting: { description: "The tree has NO `stylua.toml` and NO `.editorconfig` (verified by glob + grep). `dot_luarc.json`", lines: 31 }
  naming: { description: "Multi-word Lua files use hyphens, NEVER underscores: `lsp-debug.lua`, `memory-monitor.lua`,", lines: 38 }
  util-library: { description: "The util library is a LazyVim-derived module tree at `lua/util/`, bootstrapped as the global", lines: 45 }
plugins:
  README: { description: "Conventions for lazy.nvim plugin specs under `lua/plugins/<category>/`: spec file shape (opts vs config vs init), lazy-loading trigger selection, cross-plugin integration via User autocmds and guarded `_G` globals, and the disable-first migration pattern.", lines: 3 }
  cross-plugin-integration: { description: "Decoupled plugins talk via a named `User` autocmd, NOT direct requires. The canonical", lines: 33 }
  lazy-loading: { description: "Pick exactly one trigger by how the plugin is reached. Getting this wrong makes a plugin", lines: 36 }
  spec-shape: { description: "Every file under `lua/plugins/<category>/` MUST `return` a lazy.nvim spec: either a single", lines: 40 }
