# Plugins standards

Conventions for lazy.nvim plugin specs under `lua/plugins/<category>/`: spec file shape (opts vs config vs init), lazy-loading trigger selection, cross-plugin integration via User autocmds and guarded `_G` globals, and the disable-first migration pattern.
